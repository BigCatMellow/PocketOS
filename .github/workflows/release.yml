name: Release (distribution paused)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  distribution-paused:
    runs-on: ubuntu-latest
    steps:
      - name: Distribution hold
        run: |
          echo "PocketOS public release publishing is temporarily disabled."
          echo "Do not publish release artifacts until release safety and shipped-version truth are reconciled."
